# 030-processor

function getcolumn()
{
    RAMLN=$(grep "MEMORY" $DATA|tail -1)
    RAM=$(cut -d'+' -f2 <<< $RAMLN)
    printf "<td>$RAM</td>"
}
function getcolumnname()
{
    printf "<td>RAM (MB)</td>"
}

# vim:tabstop=4:shiftwidth=4:encoding=utf-8:expandtab:filetype=sh
